// Prettier config — carried over from the JTL-Zentralisierungs-Repo
// (feature/jtl-catalog-reference). Identical defaults across all our
// TypeScript projects to keep diffs readable.

module.exports = {
    singleQuote: true,
    trailingComma: 'all',
    semi: false,
    arrowParens: 'avoid',
    tabWidth: 4,
    printWidth: 120,
    useTabs: false,
    bracketSpacing: true,
}
